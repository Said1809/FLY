# Диаграмма бизнес-контекста (IDEF0 A-0)

Контекстная диаграмма описывает систему «Fly» как единый процесс верхнего уровня
и её взаимодействие с внешней средой.

![IDEF0 A-0](../images/idef0_a0.png)

![IDEF0 декомпозиция](../images/idef0_decomp.png)

<details>
<summary>PlantUML (исходник A-0)</summary>

```plantuml
@startuml
skinparam rectangle {
  BackgroundColor #F5F5FF
  BorderColor #3333AA
}

title IDEF0 A-0: «Fly» — планирование задач

rectangle "A0\nПланировать задачи\nи учебную деятельность" as A0

actor "Студент /\nПользователь" as User
database "Локальное\nхранилище\n(SQLite)" as DB
cloud "Supabase Auth\n(облако)" as Auth

User --> A0 : Задачи, даты,\nнастройки UI
A0 --> User : Календарь,\nсписки задач,\nстатус выполнения
A0 --> DB : CRUD задач
DB --> A0 : Список задач\nпо дате
User --> Auth : Email, пароль\n(регистрация/вход)
Auth --> User : JWT-сессия
A0 --> Auth : Проверка сессии
Auth --> A0 : Состояние\nаутентификации
@enduml
```

</details>

## Описание входов, выходов, механизмов и управления

| Элемент IDEF0 | Описание |
|---------------|----------|
| **Вход (I)** | Текст задачи, описание, дата, время, команды пользователя (создать, отметить, удалить) |
| **Выход (O)** | Отображение задач по дням, недельный/месячный календарь, персонализированный интерфейс |
| **Управление (C)** | Требования курсового проекта, методические указания, политика Supabase |
| **Механизм (M)** | Flutter-приложение, SQLite, Supabase SDK, устройство пользователя |
